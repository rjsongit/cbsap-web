import { CharacterFocusTrackerDirective } from './character-focus-tracker.directive';

describe('CharacterFocusTrackerDirective', () => {
  it('should create an instance', () => {
    const directive = new CharacterFocusTrackerDirective();
    expect(directive).toBeTruthy();
  });
});
